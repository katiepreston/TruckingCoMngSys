<%= form_for(@repair_record) do |f| %>
  <% if @repair_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@repair_record.errors.count, "error") %> prohibited this repair_record from being saved:</h2>

      <ul>
      <% @repair_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
