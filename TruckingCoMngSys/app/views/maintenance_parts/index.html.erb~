<p id="notice"><%= notice %></p>

<h1>Listing Maintenance Parts</h1>

<table>
  <thead>
    <tr>
      <th>References</th>
      <th>Total cost</th>
      <th>Quantity</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @maintenance_parts.each do |maintenance_part| %>
      <tr>
        <td><%= maintenance_part.references %></td>
        <td><%= maintenance_part.total_cost %></td>
        <td><%= maintenance_part.quantity %></td>
        <td><%= link_to 'Show', maintenance_part %></td>
        <td><%= link_to 'Edit', edit_maintenance_part_path(maintenance_part) %></td>
        <td><%= link_to 'Destroy', maintenance_part, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Maintenance part', new_maintenance_part_path %>
