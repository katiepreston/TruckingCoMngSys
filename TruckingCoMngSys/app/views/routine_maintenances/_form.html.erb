<%= form_for(@routine_maintenance) do |f| %>
  <% if @routine_maintenance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@routine_maintenance.errors.count, "error") %> prohibited this routine_maintenance from being saved:</h2>

      <ul>
      <% @routine_maintenance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
